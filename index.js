const add = (a, b) => a + b;
const subtract = (a, b) => a -b ;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;

console.log(add(1, 2));
console.log(subtract(1, 2));
console.log(multiply(1, 2));
console.log(divide(1, 2));

const person = {
    firstName: "Somboon",
    lastName: "Nontaganok",
    age: 30,
    location: "Bangkok",
    hobby: "running" 
}

console.log("Hi, my name is " + person.firstName + " " + person.lastName + ". I live in " + person.location + " and enjoy " + person.hobby + ".");

